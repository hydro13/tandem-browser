node_modules/
dist/
release/
*.lock
*.log
.playwright-mcp/
coverage/
src/security/blocklists/data/

# Native binaries (compiled locally)
native/speech/tandem-speech

# macOS system files
.DS_Store

# Cowork workspace (local only, not for the repo)
.cowork/
